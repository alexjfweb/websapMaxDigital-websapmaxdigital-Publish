import * as React from "react";
import { Twitter } from "lucide-react"; // Using Lucide's Twitter icon for X

export default function XIcon(props: React.SVGProps<SVGSVGElement>) {
  return <Twitter {...props} />;
}

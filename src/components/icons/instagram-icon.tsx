import * as React from "react";
import { Instagram } from "lucide-react"; // Using Lucide's icon

export default function InstagramIcon(props: React.SVGProps<SVGSVGElement>) {
  return <Instagram {...props} />;
}

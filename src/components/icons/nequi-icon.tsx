import * as React from "react";
import { CreditCard } from "lucide-react"; // Using Lucide's CreditCard as a placeholder

export default function NequiIcon(props: React.SVGProps<SVGSVGElement>) {
  // Using a generic payment icon as a placeholder for Nequi
  // In a real app, you'd use the actual Nequi logo SVG
  return <CreditCard {...props} />;
}

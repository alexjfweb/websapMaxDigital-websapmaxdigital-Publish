import * as React from "react";
import { Facebook } from "lucide-react"; // Using Lucide's icon

export default function FacebookIcon(props: React.SVGProps<SVGSVGElement>) {
  return <Facebook {...props} />;
}
